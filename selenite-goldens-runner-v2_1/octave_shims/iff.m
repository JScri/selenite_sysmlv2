% Hoisted from SELENITE_ECON_V1_3.m for Octave (script-local functions unsupported).
function result = iff(condition, trueVal, falseVal)
    if condition, result = trueVal; else, result = falseVal; end
end
